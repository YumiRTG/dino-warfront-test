name: Bootstrap test repository from app

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/bootstrap-from-app.yml'

permissions:
  contents: write

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout test repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout source repository
        uses: actions/checkout@v4
        with:
          repository: YumiRTG/app
          ref: main
          path: _source
          fetch-depth: 1
          lfs: true

      - name: Replace test contents with source snapshot
        shell: bash
        run: |
          find . -mindepth 1 -maxdepth 1 ! -name '.git' ! -name '_source' -exec rm -rf {} +
          rsync -a --delete --exclude='.git/' _source/ ./
          rm -rf _source

      - name: Commit synchronized snapshot
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if git diff --cached --quiet; then
            echo 'No changes to commit.'
          else
            git commit -m 'Sync test baseline from YumiRTG/app'
            git push origin HEAD:main
          fi
